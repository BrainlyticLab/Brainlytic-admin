# Brainlytic Admin App

## Run The App

Use Node `v16.9.0`. Use `nvm` to manage node versions.

- Install the dependencies

    ```bash
    npm install
    ```

- Run the app

    ```bash
    npm start
    ```
